spring.application.name=itjob
server.port=8080

spring.profiles.active=prod

# Comma-separated list of allowed CORS origins
app.cors.allowed-origins=http://localhost:5173

# Make cloudinary optional in dev (fallback to URL-only)
# cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dummy}
# cloudinary.api-key=${CLOUDINARY_API_KEY:dummy}
# cloudinary.api-secret=${CLOUDINARY_API_SECRET:dummy}
# cloudinary.resume-folder=${CLOUDINARY_RESUME_FOLDER:itjob-portal/resumes}

# # File upload limits
# spring.servlet.multipart.max-file-size=10MB
# spring.servlet.multipart.max-request-size=10MB
